package me.hyperburger.joinplugin.messages;

public enum MessageType {
    CREATE_GROUP,
    GROUP_PERMISSION,
    // Message
    JOIN_MESSAGE,
    LEAVE_MESSAGE,
    // Sound
    JOIN_SOUND,
    // Command
    JOIN_COMMAND,
    // Title
    JOIN_TITLE,
    JOIN_SUBTITLE,
    JOIN_TITLE_FADEIN,
    JOIN_TITLE_STAY,
    JOIN_TITLE_FADEOUT,
    // ActionBar
    JOIN_ACTIONBAR,
    JOIN_ACTIONBAR_DURATION,
    // BossBar
    JOIN_BOSSBAR,
    JOIN_BOSSBAR_STYLE,
    JOIN_BOSSBAR_COLOR,
    JOIN_BOSSBAR_DURATION;
}
